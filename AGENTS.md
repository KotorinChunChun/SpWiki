# SpWiki 開発ルール

## 作業の原則

- 質問には回答だけを行い、明確な指示がない限りデータを変更しない。
- PowerShell は必ず pwsh を使用する。文書とコミットメッセージは日本語で残す。
- この目録から目的に必要な資料だけを読む。依存パッケージ、生成物、非公開資料を通常の調査に含めない。
- まとまった変更はフェーズを分け、関連するテスト・ビルドを確認してからコミットする。
- ユーザーに動作確認を求める場合は、起動中でなければ cd と起動コマンドを示す。

## 配置

| 場所 | 用途 |
| --- | --- |
| `README.md` | 製品概要と文書への入口 |
| `docs/` | 利用者・管理者・公開開発者向け文書 |
| `src/` | SPFx ソースと同梱リソース |
| `config/` | SPFx 標準のビルド設定 |
| `tests/*.test.mjs` | 単体テスト |
| `tests/fixtures/documents/` | テスト・ローカル表示用のサンプル文書 |
| `tests/harness/` | ブラウザーでの確認画面 |
| `dev/scripts/` | ビルド・検証・配布用スクリプト |
| `dev/` | 開発ルールと作業資料 |
| `packages/` | バージョン別の配布物。Git 対象外 |

SPFx が使用する `gulpfile.js`、`tsconfig.json`、`package*.json`、設定ファイルはルートに置く。
`.tools/` はポータブル Node、`temp/`・`lib/`・`dist/`・`release/` はビルド生成物として Git 対象外にする。
`src/webparts/spWiki/core/bundledDocs.ts` は追跡する生成物。文書を変更したら `npm run docs:build` で再生成する。
`.claude/` はローカル設定として公開対象から除外し、`.vscode/` は共有用設定だけを置く。

秘密情報・個人情報は `dev/private/`、プロジェクト外の一時制作物は `dev/temp/` に置く。
両者は Git・公開成果物・通常の調査から除外し、ユーザーが明示した資料だけを読む。

## 目的別目録

| 目的 | 資料 |
| --- | --- |
| 製品概要 | [README](README.md) |
| 閲覧・編集・管理 | [文書一覧](docs/README.md)から該当ガイド |
| 環境構築・改造 | [開発者ガイド](docs/DEVELOPERS_GUIDE.md) |
| ビルドコマンド | [コマンド一覧](docs/BUILD_COMMANDS.md) |
| 技術上の注意事項 | [技術リファレンス](docs/TECHNICAL_REFERENCE.md) |
| 配布 | [リリースルール](dev/RELEASE_RULE.md) |
| 作業資料 | [開発資料の入口](dev/README.md) |

## 要求・実装資料

ユーザーの要求は `dev/vX.Y.Z-req.md`、AI の計画・報告は `dev/vX.Y.Z-imp.md`、
細分化が必要な進捗は `dev/vX.Y.Z-task.md` に置く。必要な場合にだけ作成する。
作業中は複数版とも dev 直下、完了した一式は `dev/history/vX.Y.Z/` に移す。
未完了事項は出典と引き継ぎ先の双方に記録する。版の開始・リリースだけで完了とは判断しない。
過去の未チェック欄は後続の完了記録・ユーザー確認と照合し、新しい測定証跡とは区別する。
テンプレートを取り込む場合は固有ルールを維持して統合し、取り込み用コピーは削除する。

コミットは `docs:`、`chore:`、`fix:` などの接頭辞と日本語の要約を使う。
